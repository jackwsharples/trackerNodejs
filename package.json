{
  "name": "tracker-nodejs",
  "private": true,
  "version": "1.0.0",
  "description": "HTTP API for GPS tracker (Express + Prisma)",
  "main": "app.js",
  "scripts": {
    "postinstall": "prisma generate",
    "migrate:deploy": "prisma migrate deploy",
    "start": "node app.js",
    "dev": "nodemon app.js"
  },
  "dependencies": {
    "@prisma/client": "^6.16.2",
    "express": "^4.19.2"
  },
  "devDependencies": {
    "nodemon": "^3.1.0",
    "prisma": "^6.16.2",
    "vite": "^7.1.7"
  },
  "engines": {
    "node": ">=18"
  }
}
